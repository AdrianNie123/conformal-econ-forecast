"""Full benchmark pipeline — runs all models on all series across all regimes.

Week 3 implementation. Stub only.

Usage (when implemented):
    python -m conformal_econ.benchmark.runner \\
        --series all --models all --alpha 0.1 \\
        --output results/benchmark_results.parquet
"""

# TODO (Week 3): Orchestrate full benchmark. Save results to parquet.
#                Dashboard loads pre-computed results — no re-running for demo.
